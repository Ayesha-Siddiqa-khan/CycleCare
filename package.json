{
  "name": "cyclecare",
  "private": true,
  "description": "Private, simple menstrual cycle tracking web app",
  "scripts": {
    "lint": "node --check app.js && node --check tests.js",
    "test": "node tests.js",
    "build": "node -e \"require('fs').accessSync('index.html'); require('fs').accessSync('styles.css'); require('fs').accessSync('app.js')\""
  }
}
